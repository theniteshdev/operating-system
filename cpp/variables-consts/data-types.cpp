#include <iostream>
#include <climits>

using namespace std;
// Datatypes in Cpp

int main()
{
    // primitive datatypes
    // derived datatypes -> array, function, pointer, reference
    // user-defined -> structure(almost they are classes), union, enum, class, typedef

    // experiments with int datatype (The range for a signed 32-bit integer is from -2,147,483,648 to 2,147,483,647.)

    // float (It can represent values in the approximate range of 1.2e-38 to 3.4e+38. [A float offers approximately 6-7 significant decimal digits of precision.])

    // double (The maximum value that a double data type can store in C++ is approximately 1.8 x 10³⁰⁸. This is a finite value, but numbers can go higher to positive infinity if an operation exceeds this limit. )
    cout << INT_MAX << endl;

    return 0;
}